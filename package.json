{
  "scripts": {
    "build": "node scripts/build-static.js",
    "check:meta": "node scripts/check-meta.js",
    "check:sitemap": "node scripts/check-sitemap.js",
    "check:links": "node scripts/check-links.js",
    "check:images": "node scripts/check-images.js",
    "check:atlas": "node scripts/check-atlas.js",
    "check:editorial": "node scripts/check-editorial.js",
    "check": "npm run check:meta && npm run check:sitemap && npm run check:links && npm run check:images && npm run check:atlas && npm run check:editorial",
    "preview": "python3 -m http.server 4173 -d dist"
  },
  "devDependencies": {}
}
